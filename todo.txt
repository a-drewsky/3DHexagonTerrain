

-REFACTOR
    -table should be a seperate component
    -create a render map for each rotation and zoom level when object is created
    -organize draw interval
    -fix colors
    -initial camera position
    -Modulate function to calculate camera position
    -Options for map size and camera starting position
    -Refactor game dev template and take a break from coding
    -Change stoke size based on zoom level


BUGS
    -Rotating while moving resets camera to the last position before moving


-climate generation
    -elevation
    -temperature
    -dryness/wetness
    -lakes and waterfalls
        -generate rivers and lakes
        -water tiles should not be on the edge (like a cliff) unless connecting to a water tile below
        -hexagon wall colors should be a waterfall if connecting water tiles
        